# KeyAuth Enterprise Source Code
Diwnxss bought the source code off KeyAuth for $200. Now released for free!

# What's in it?
> All premium features they sell! (Seller subscription, reseller panel, etc...)

# What's bad?
> The API endpoint is still 1.2's latest version before the 1.3 update release. 1.2 also has a vulnerability with their system (KeyAuth register/login Emulation) which can be patched which simple checks.
